# Load necessary libraries
library(readxl)
library(dplyr)

# Load the raw data
raw_data <- read_excel("data/Raw data.xlsx")

# Inspect the structure of the data
str(raw_data)
head(raw_data)

# Save the loaded data into an RDS file for further use
saveRDS(raw_data, "data/raw_data.rds")

